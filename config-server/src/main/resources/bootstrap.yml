spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BrightStarry/SpringCloud/tree/master/config-repository/src/main/resources
          default-label: master
          username:
          password:
server:
  port: 6666
